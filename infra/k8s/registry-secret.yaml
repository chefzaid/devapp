apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: devapp-registry-auth
  namespace: apps
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: devapp-registry-auth
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: >-
          {"auths":{"{{ .registry }}":{"username":"{{ .username }}","password":"{{ .password }}","auth":"{{ printf "%s:%s" .username .password | b64enc }}"}}}
  data:
    - secretKey: registry
      remoteRef:
        key: apps/devapp/registry
        property: registry
    - secretKey: username
      remoteRef:
        key: apps/devapp/registry
        property: username
    - secretKey: password
      remoteRef:
        key: apps/devapp/registry
        property: password
