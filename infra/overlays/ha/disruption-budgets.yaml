apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: user-app
spec:
  minAvailable: 1
  unhealthyPodEvictionPolicy: AlwaysAllow
  selector:
    matchLabels:
      app: user-app
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: order-app
spec:
  minAvailable: 1
  unhealthyPodEvictionPolicy: AlwaysAllow
  selector:
    matchLabels:
      app: order-app
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: devapp-web
spec:
  minAvailable: 1
  unhealthyPodEvictionPolicy: AlwaysAllow
  selector:
    matchLabels:
      app: devapp-web
